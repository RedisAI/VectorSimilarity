name: benchmark (labeled)

on: # TODO: delete this file when specifying a label is supported
  pull_request:
    types:
      - labeled
    branches-ignore:
      - '*docs'
      - '*noci'

jobs:
  bm_invoker: 
    uses: ./.github/workflows/benchmark.yml
    with:
      git_label: ${{ github.event.label.name }}
    secrets: inherit
