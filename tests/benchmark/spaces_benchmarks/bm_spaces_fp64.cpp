/*
 *Copyright Redis Ltd. 2021 - present
 *Licensed under your choice of the Redis Source Available License 2.0 (RSALv2) or
 *the Server Side Public License v1 (SSPLv1).
 */
#define DATA_TYPE double
#include "bm_spaces.h"

// AVX512 functions
#ifdef __AVX512F__
#include "VecSim/spaces/L2/L2_AVX512.h"
#include "VecSim/spaces/IP/IP_AVX512.h"

INITIALIZE_EXACT_512BIT_BM(FP64, AVX512_F, L2, 8, FP64_L2SqrSIMD8Ext_AVX512);
INITIALIZE_EXACT_128BIT_BM(FP64, AVX512_F, L2, 2, FP64_L2SqrSIMD8Ext_AVX512);
INITIALIZE_RESIDUAL_BM(FP64, AVX512_F, L2, 8_Residuals, FP64_L2SqrSIMD8Ext_AVX512);

INITIALIZE_EXACT_512BIT_BM(FP64, AVX512_F, IP, 8, FP64_InnerProductSIMD8Ext_AVX512);
INITIALIZE_EXACT_128BIT_BM(FP64, AVX512_F, IP, 2, FP64_InnerProductSIMD8Ext_AVX512);
INITIALIZE_RESIDUAL_BM(FP64, AVX512_F, IP, 8_Residuals, FP64_InnerProductSIMD8Ext_AVX512);

#endif // AVX512F

// AVX functions
#ifdef __AVX__
#include "VecSim/spaces/L2/L2_AVX.h"
#include "VecSim/spaces/IP/IP_AVX.h"

INITIALIZE_EXACT_512BIT_BM(FP64, AVX, L2, 8, FP64_L2SqrSIMD8Ext_AVX);
INITIALIZE_EXACT_128BIT_BM(FP64, AVX, L2, 2, FP64_L2SqrSIMD8Ext_AVX);
INITIALIZE_RESIDUAL_BM(FP64, AVX, L2, 8_Residuals, FP64_L2SqrSIMD8Ext_AVX);

INITIALIZE_EXACT_512BIT_BM(FP64, AVX, IP, 8, FP64_InnerProductSIMD8Ext_AVX);
INITIALIZE_EXACT_128BIT_BM(FP64, AVX, IP, 2, FP64_InnerProductSIMD8Ext_AVX);
INITIALIZE_RESIDUAL_BM(FP64, AVX, IP, 8_Residuals, FP64_InnerProductSIMD8Ext_AVX);
#endif // AVX

// SSE functions
#ifdef __SSE__
#include "VecSim/spaces/L2/L2_SSE.h"
#include "VecSim/spaces/IP/IP_SSE.h"

INITIALIZE_EXACT_512BIT_BM(FP64, SSE, L2, 8, FP64_L2SqrSIMD8Ext_SSE);
INITIALIZE_EXACT_128BIT_BM(FP64, SSE, L2, 2, FP64_L2SqrSIMD8Ext_SSE);
INITIALIZE_RESIDUAL_BM(FP64, SSE, L2, 8_Residuals, FP64_L2SqrSIMD8Ext_SSE);

INITIALIZE_EXACT_512BIT_BM(FP64, SSE, IP, 8, FP64_InnerProductSIMD8Ext_SSE);
INITIALIZE_EXACT_128BIT_BM(FP64, SSE, IP, 2, FP64_InnerProductSIMD8Ext_SSE);
INITIALIZE_RESIDUAL_BM(FP64, SSE, IP, 8_Residuals, FP64_InnerProductSIMD8Ext_SSE);

#endif // SSE
// Naive algorithms

#include "VecSim/spaces/L2/L2.h"
#include "VecSim/spaces/IP/IP.h"

INITIALIZE_NAIVE_BM(FP64, InnerProduct)->EXACT_512BIT_PARAMS->RESIDUAL_PARAMS;
INITIALIZE_NAIVE_BM(FP64, L2Sqr)->EXACT_512BIT_PARAMS->RESIDUAL_PARAMS;

// Naive

BENCHMARK_MAIN();

#include "VecSim/spaces/space_chooser_cleanup.h"
