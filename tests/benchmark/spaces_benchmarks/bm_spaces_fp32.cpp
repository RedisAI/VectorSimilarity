/*
 *Copyright Redis Ltd. 2021 - present
 *Licensed under your choice of the Redis Source Available License 2.0 (RSALv2) or
 *the Server Side Public License v1 (SSPLv1).
 */
#define DATA_TYPE float
#include "bm_spaces.h"

// AVX512 functions
#ifdef __AVX512F__

INITIALIZE_EXACT_512BIT_BM(FP32, AVX512_F, L2, 16);
INITIALIZE_EXACT_128BIT_BM(FP32, AVX512_F, L2, 4);
INITIALIZE_RESIDUAL_BM(FP32, AVX512_F, L2, 16_Residuals);

INITIALIZE_EXACT_512BIT_BM(FP32, AVX512_F, IP, 16);
INITIALIZE_EXACT_128BIT_BM(FP32, AVX512_F, IP, 4);
INITIALIZE_RESIDUAL_BM(FP32, AVX512_F, IP, 16_Residuals);
#endif // AVX512F

// AVX functions
#ifdef __AVX__

INITIALIZE_EXACT_512BIT_BM(FP32, AVX, L2, 16);
INITIALIZE_EXACT_128BIT_BM(FP32, AVX, L2, 4);
INITIALIZE_RESIDUAL_BM(FP32, AVX, L2, 16_Residuals);

INITIALIZE_EXACT_512BIT_BM(FP32, AVX, IP, 16);
INITIALIZE_EXACT_128BIT_BM(FP32, AVX, IP, 4);
INITIALIZE_RESIDUAL_BM(FP32, AVX, IP, 16_Residuals);
#endif // AVX

// SSE functions
#ifdef __SSE__

INITIALIZE_EXACT_512BIT_BM(FP32, SSE, L2, 16);
INITIALIZE_EXACT_128BIT_BM(FP32, SSE, L2, 4);
INITIALIZE_RESIDUAL_BM(FP32, SSE, L2, 16_Residuals);

INITIALIZE_EXACT_512BIT_BM(FP32, SSE, IP, 16);
INITIALIZE_EXACT_128BIT_BM(FP32, SSE, IP, 4);
INITIALIZE_RESIDUAL_BM(FP32, SSE, IP, 16_Residuals);

#endif // SSE

// Naive algorithms

INITIALIZE_NAIVE_BM(FP32, InnerProduct)->EXACT_512BIT_PARAMS->RESIDUAL_PARAMS;
INITIALIZE_NAIVE_BM(FP32, L2Sqr)->EXACT_512BIT_PARAMS->RESIDUAL_PARAMS;

// Naive

BENCHMARK_MAIN();
